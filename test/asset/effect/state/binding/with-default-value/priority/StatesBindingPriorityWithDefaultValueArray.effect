{
	"states" : {
        "priority" : { "binding" : "material[${materialUuid}].priority", "default" : ["first", 42.0] }
    },

    "techniques" : [{ "passes" : [{
      "vertexShader" : "#pragma include \"../../../../dummy.glsl\"",
      "fragmentShader" : "#pragma include \"../../../../dummy.glsl\""
  }] }]
}
